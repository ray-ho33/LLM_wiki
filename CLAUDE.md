# LLM Wiki — 프로젝트 관리 지식 베이스

이 디렉토리는 LLM이 유지·관리하는 마크다운 위키입니다.
아래 규칙을 따라 위키를 운영하세요.

---

## 구조

```
LLM_wiki/
├── CLAUDE.md          ← 이 파일. 위키 운영 규칙.
├── .mcp.json          ← kordoc MCP 서버 설정
├── raw/               ← 원본 소스 (불변, 절대 수정하지 않음)
│   ├── assets/        ← 이미지, 첨부파일
│   └── done/          ← 파싱 완료된 소스 (ingest 후 이동)
├── wiki/              ← LLM이 관리하는 위키 페이지
│   ├── index.md       ← 전체 페이지 카탈로그
│   ├── log.md         ← 작업 로그 (시간순)
│   ├── overview.md    ← 위키 현황 대시보드
│   ├── sources/       ← 소스별 요약
│   ├── projects/      ← 프로젝트별 페이지
│   ├── entities/      ← 조직, 인물, 시스템 등
│   ├── concepts/      ← 주요 개념, 용어 정의
│   ├── reports/       ← 분석, 비교, 질의 결과
│   └── issues/        ← 이슈, 리스크, 의사결정
├── templates/         ← 양식 원본 (읽기 전용)
├── output/            ← 채워진 양식 결과물
└── tools/             ← (선택) 검색 스크립트 등
```

### 핵심 원칙

- `raw/` 는 **읽기 전용**. 원본 파일 내용을 절대 수정하지 않는다.
- `raw/done/` 은 파싱이 완료된 소스를 옮겨두는 폴더. ingest 완료 후 원본을 이곳으로 이동한다.
- `wiki/` 는 **LLM이 전적으로 관리**. 페이지 생성, 수정, 삭제 모두 LLM이 한다.
- `templates/` 는 **읽기 전용**. 양식 원본은 수정하지 않는다.
- `output/` 는 **LLM이 채운 양식 결과물**을 저장한다.
- 사용자는 소스를 `raw/`에, 양식을 `templates/`에 넣고, 질문을 하고, 방향을 잡는다. LLM이 나머지를 한다.

---

## 페이지 컨벤션

### Frontmatter

모든 위키 페이지는 YAML frontmatter로 시작한다:

```yaml
---
title: 페이지 제목
type: source | project | entity | concept | report | issue
tags: [태그1, 태그2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [관련 소스 파일명]
status: active | completed | archived  # project, issue에만 사용
---
```

### 파일 이름

- 한글 사용 가능. 공백 대신 언더바(`_`) 사용.
- 예: `wiki/projects/신규_시스템_구축.md`, `wiki/entities/기획팀.md`

### 내부 링크

- `[[페이지명]]` 형식 사용 (Obsidian 호환).
- 다른 폴더의 페이지: `[[projects/신규_시스템_구축]]`
- 링크 생성 시, 대상 페이지가 없으면 만든다.

### 태그 체계

소스 문서의 성격에 따라 태그를 부여한다. 예시:
- 문서 유형: `보고서`, `회의록`, `계획서`, `현황표`, `지침`, `공문`
- 업무 분야: 프로젝트/도메인에 따라 자유롭게 추가
- 상태: `진행중`, `완료`, `보류`

---

## 워크플로우

### 1. Ingest (소스 투입)

사용자가 `raw/`에 새 문서를 넣고 처리를 요청하면:

1. **파싱**: kordoc CLI 또는 MCP로 문서를 읽는다.
   - CLI: `kordoc <파일경로>` → 마크다운 출력
   - MCP: kordoc MCP 도구 사용
   - 이미지: Read 도구로 직접 열람
2. **사용자와 논의**: 핵심 내용을 요약하고, 어떤 점이 중요한지 사용자와 확인한다.
3. **소스 요약 페이지 작성**: `wiki/sources/` 에 요약 페이지 생성.
   - 문서 메타정보 (제목, 날짜, 출처, 유형)
   - 핵심 내용 요약
   - 주요 데이터/수치
   - 관련 위키 페이지 링크
4. **관련 페이지 업데이트**: 소스에서 언급된 프로젝트, 엔티티, 개념 등의 기존 페이지를 업데이트한다. 없으면 새로 만든다.
5. **인덱스 갱신**: `wiki/index.md`에 새 페이지들을 등록한다.
6. **로그 기록**: `wiki/log.md`에 작업 내역을 추가한다.
7. **overview 갱신**: `wiki/overview.md`의 현황 수치와 최근 활동을 업데이트한다.
8. **소스 이동**: 파싱이 완료된 원본 파일을 `raw/done/`으로 이동한다.

### 2. Query (질의)

사용자가 질문하면:

1. **인덱스 탐색**: `wiki/index.md`를 읽고 관련 페이지를 식별한다.
2. **페이지 읽기**: 관련 페이지들을 읽고 정보를 종합한다.
3. **답변 생성**: 출처를 명시하며 답변한다. `[출처: [[페이지명]]]` 형식.
4. **(선택) 결과 저장**: 가치 있는 분석 결과는 `wiki/reports/`에 저장하고 인덱스에 등록한다.

### 3. Fill (양식 채우기)

사용자가 `templates/`에 양식을 넣고 채우기를 요청하면:

1. **템플릿 파싱**: kordoc CLI 또는 MCP로 양식을 마크다운으로 변환하고, 빈 칸(슬롯)을 식별한다.
2. **위키 탐색**: `wiki/index.md`에서 관련 페이지를 찾아 슬롯에 필요한 정보를 수집한다.
3. **원본 보충**: 위키에 없는 정보는 `raw/` 원본에서 직접 파싱하여 추출한다.
4. **슬롯 매칭**: 수집한 정보를 빈 칸에 매칭한다.
   - 확인되지 않는 항목은 **`【확인 필요】`** 로 표시한다. 절대 추측하지 않는다.
5. **미리보기**: 채워진 결과를 마크다운 테이블로 보여주고 사용자 확인을 받는다.
   - 각 슬롯에 **출처**(어떤 위키 페이지에서 가져왔는지)를 함께 표시한다.
   - 출처는 미리보기 전용이며 최종 출력에는 포함하지 않는다.

   ```
   | 슬롯      | 채운 값          | 출처                         |
   |----------|-----------------|------------------------------|
   | 소속부서   | 사회민원갈등조정과 | [[entities/사회민원갈등조정과]] |
   | 출장일자   | 【확인 필요】     | —                            |
   ```

6. **출력**: 사용자 확인 후 `output/`에 저장한다.
   - `.hwpx` 양식 → kordoc 역변환으로 `.hwpx` 출력
   - 기타 양식 → 마크다운으로 출력
   - 파일명: `<양식명>_<YYYY-MM-DD>.<확장자>`
7. **로그 기록**: `wiki/log.md`에 fill 작업 내역을 추가한다.

#### Fill 규칙

- `templates/` 원본은 **절대 수정하지 않는다** (raw/와 동일 원칙).
- 확인 안 된 정보를 **추측하여 채우지 않는다** (hallucination 방지).
- 미리보기 없이 **바로 출력하지 않는다** (사용자 확인 필수).
- 출력 파일명에 날짜를 포함하여 **동일 양식 반복 사용을 지원**한다.

### 4. Lint (건강 점검)

사용자가 점검을 요청하거나 주기적으로:

1. **모순 확인**: 페이지 간 상충하는 정보 찾기
2. **고아 페이지**: 다른 페이지에서 링크되지 않은 페이지 찾기
3. **빈 링크**: `[[...]]` 링크 중 대상 페이지가 없는 것 찾기
4. **오래된 정보**: 오랫동안 업데이트되지 않은 페이지 확인
5. **누락된 페이지**: 자주 언급되지만 전용 페이지가 없는 개념/엔티티
6. **데이터 갭**: 추가 소스가 필요한 영역 제안
7. 결과를 보고하고, 사용자 승인 후 수정한다.

---

## kordoc 사용법

### CLI로 파싱
```bash
# 단일 파일 → stdout에 마크다운 출력
kordoc raw/보고서.hwpx

# 특정 페이지만
kordoc raw/문서.pdf --pages 1-3

# 디렉토리 내 모든 문서 변환
kordoc raw/*.hwpx --out-dir wiki/sources/
```

### 지원 형식
- HWP, HWPX → 마크다운
- PDF (텍스트/OCR) → 마크다운
- XLSX → 마크다운 테이블
- DOCX → 마크다운

---

## index.md 관리 규칙

각 항목은 한 줄로 작성:
```
- [[sources/소스_파일명]] — 핵심 한줄 설명 (YYYY-MM-DD)
```

카테고리별로 정리하고, 새 페이지 추가 시 해당 카테고리에 삽입한다.
200개 이상이 되면 카테고리별 하위 인덱스 파일로 분리한다.

## log.md 관리 규칙

각 항목은 다음 형식:
```
## [YYYY-MM-DD] 작업유형 | 제목
- 수행한 작업 내역
- 업데이트한 페이지 목록
```

작업유형: `ingest`, `query`, `fill`, `lint`, `update`, `init`
최신 항목이 아래로 추가된다 (append-only).
